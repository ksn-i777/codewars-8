function fakeBin(x){
    const arr = x.split('');
    let arr2 = [];
    let str = '';
    for (let i=0; i<arr.length; i++) {
      if (arr[i] < 5) {
        arr2.push(0);
      } else {
        arr2.push(1);
      }       
    }
    str = arr2.join().replace(/[\s.,%]/g, '');
    return str;
  }
